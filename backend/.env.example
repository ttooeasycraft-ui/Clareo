# Copy to .env for local development
# No required secrets — Whisper runs locally, yt-dlp is open-source

# Optional: override Whisper model (tiny|base|small|medium|large)
# Larger = more accurate but slower. "small" is a good default.
WHISPER_MODEL=small
